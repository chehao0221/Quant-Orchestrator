name: Crypto Genius

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Crypto AI
        run: python repos/Stock-Genius-System/scripts/ai_crypto_post.py

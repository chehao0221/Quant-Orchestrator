name: Stock Genius US System 🇺🇸

on:
  workflow_dispatch:
  schedule:
    # 🇺🇸 美股盤後 1 小時
    # 美股約 16:00 ET 收盤 ≈ UTC 21:00 / 夏令時間
    # 保守設 06:30 UTC（你目前用的時間，OK）
    - cron: "30 6 * * 1-5"

jobs:
  guardian-gate:
    name: 🛡 Guardian Pause Gate
    runs-on: ubuntu-latest
    outputs:
      allowed: ${{ steps.check.outputs.allowed }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Check Guardian State
        id: check
        run: |
          python shared/check_guardian_gate.py

  genius-us:
    name: 📊 Stock Genius US
    needs: guardian-gate
    if: needs.guardian-gate.outputs.allowed == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r repos/Stock-Genius-System/requirements.txt

      - name: Run US AI Prediction
        env:
          DISCORD_WEBHOOK_US: ${{ secrets.DISCORD_WEBHOOK_US }}
        run: |
          python repos/Stock-Genius-System/scripts/ai_us_post.py

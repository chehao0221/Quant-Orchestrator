name: Stock Genius System

on:
  schedule:
    # 每天盤後（台灣 18:30 = UTC 10:30）
    - cron: "30 10 * * *"
  workflow_dispatch:

jobs:
  genius:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # ============================
      # Guardian 風控檢查（關鍵）
      # ============================
      - name: Check Guardian Risk State
        id: guardian_check
        run: |
          python - << 'EOF'
          import json, sys, pathlib

          state_file = pathlib.Path("shared/guardian_state.json")
          if not state_file.exists():
              print("⚠️ 找不到 guardian_state.json，視為允許執行")
              sys.exit(0)

          state = json.loads(state_file.read_text(encoding="utf-8"))

          if not state.get("allow_trading", True):
              print("⛔ Guardian 判定今日停盤")
              print(f"風險等級：{state.get('risk_level')}")
              sys.exit(100)

          print("✅ Guardian 允許交易，繼續執行")
          EOF

      # ============================
      # 今日停盤 → 通知 + 結束
      # ============================
      - name: Notify Trading Halt
        if: failure() && steps.guardian_check.outcome == 'failure'
        env:
          DISCORD_WEBHOOK_GENERAL: ${{ secrets.DISCORD_WEBHOOK_GENERAL }}
        run: |
          python - << 'EOF'
          import os, json, urllib.request

          webhook = os.getenv("DISCORD_WEBHOOK_GENERAL")
          if not webhook:
              print("⚠️ 未設定 Discord Webhook")
              exit(0)

          state = json.load(open("shared/guardian_state.json", encoding="utf-8"))

          payload = {
              "content": (
                  "🛑 **Stock-Genius 今日停盤**\n\n"
                  f"風險等級：{state.get('risk_level')}\n"
                  f"動作：{state.get('action')}\n"
                  "原因：Guardian 風控系統判定\n"
              )
          }

          req = urllib.request.Request(
              webhook,
              data=json.dumps(payload).encode("utf-8"),
              headers={"Content-Type": "application/json"}
          )
          urllib.request.urlopen(req)
          EOF

      # ============================
      # 正常執行 Stock-Genius
      # ============================
      - name: Install dependencies
        if: success()
        run: |
          python -m pip install --upgrade pip
          pip install -r repos/Stock-Genius-System/requirements.txt

      - name: Run TW AI
        if: success()
        run: python repos/Stock-Genius-System/scripts/ai_tw_post.py

      - name: Run US AI
        if: success()
        run: python repos/Stock-Genius-System/scripts/ai_us_post.py

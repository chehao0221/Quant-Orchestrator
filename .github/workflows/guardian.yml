name: Guardian System

on:
  schedule:
    # 🛡 盤後風控 / 黑天鵝（UTC 21:30 ≈ 台灣 05:30，可自行調）
    - cron: '30 21 * * *'
  workflow_dispatch:

jobs:
  guardian:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Orchestrator
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Guardian dependencies
        run: |
          cd repos/Quant-Guardian-Ultra
          pip install -r requirements.txt

      - name: Run Quant-Guardian-Ultra (CORRECT MODE)
        run: |
          cd repos/Quant-Guardian-Ultra
          python entrypoint.py

name: Guardian System

on:
  schedule:
    # 🛡 Guardian 系統（每日一次即可，例：UTC 21:30 = 台灣 05:30）
    - cron: '30 21 * * *'
  workflow_dispatch:

jobs:
  guardian:
    runs-on: ubuntu-latest

    steps:
      # ===============================
      # Checkout Orchestrator Repo
      # ===============================
      - name: Checkout
        uses: actions/checkout@v4

      # ===============================
      # Python Environment
      # ===============================
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      # ===============================
      # Install Guardian Dependencies
      # ===============================
      - name: Install dependencies
        run: |
          cd repos/Quant-Guardian-Ultra
          pip install -r requirements.txt

      # ===============================
      # Run Guardian (MODULE MODE ✅)
      # ===============================
      - name: Run Guardian System
        run: |
          cd repos/Quant-Guardian-Ultra
          python -m entrypoint

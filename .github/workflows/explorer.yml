name: Quant Explorer (Lv2)

on:
  schedule:
    # 🧭 Explorer 股池更新（每天一次即可）
    - cron: '30 00 * * *'   # UTC 00:30（台灣 08:30）

    # 🔍 Explorer AI 探索（每天一次）
    - cron: '00 01 * * *'   # UTC 01:00（台灣 09:00）

  workflow_dispatch:

jobs:
  explorer_tasks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Explorer Repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt

      # ===============================
      # 1️⃣ 更新 Explorer 股池（只寫 JSON）
      # ===============================
      - name: Update TW Explorer Pool
        run: |
          python scripts/update_tw_explorer_pool.py

      - name: Update US Explorer Pool
        run: |
          python scripts/update_us_explorer_pool.py

      # ===============================
      # 2️⃣ Explorer AI（只顯示 Top 5）
      # ===============================
      - name: Run TW Explorer AI
        run: |
          python scripts/ai_tw_explorer_post.py

      - name: Run US Explorer AI
        run: |
          python scripts/ai_us_explorer_post.py
